1. **什么是泛型**

   通俗的讲，泛型就是“**类型的参数**”。就像函数接收值作为参数一样，泛型允许我们在定义函数、类或接口时，把“类型”也当作参数传进来，从而实现类型的动态绑定。

2. 有什么优势
   - 安全的复用代码：写通用逻辑时不用再写 `any`，既能适配所有类型，又能保留完整的类型检查和智能提示。
   - 类型约束
   - 把校验前置到编译期

> 一句话总结：泛型让我们在不牺牲类型安全的前提下，写出了真正通用且可维护的代码。
